plugins {
    id("me.champeau.paseq")
}

paseq {
    commands {
        hello {
            command = ["ls", "-al"]
        }
        world {
            async = true
            command = [file("long.sh").absolutePath]
        }
    }
}
